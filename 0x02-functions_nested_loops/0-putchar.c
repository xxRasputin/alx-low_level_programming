#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char putch[10] = "_putchar";

	int n = 0;

	while (i < 8)
	{
		_putchar(putch[i]);
		i++;
	}
	_putchar(10);
	return (0);
}
